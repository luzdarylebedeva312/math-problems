fn main() {
    // Define variables
    let x = 3.14;
    let y = 5.0;

    // Perform mathematical operations and print the result
    println!("The area of a rectangle with length {} and width {}", y, x * y);
}
